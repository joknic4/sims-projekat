<Window x:Class="HotelReservationSystem.Views.VlasnikWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:HotelReservationSystem.Views"
        xmlns:vm="clr-namespace:HotelReservationSystem.ViewModels"
        Title="Portal za vlasnike" 
        Height="700" 
        Width="1000"
        WindowStartupLocation="CenterScreen">
    
    <Window.DataContext>
        <vm:VlasnikViewModel/>
    </Window.DataContext>
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <Border Grid.Row="0" Background="#2196F3" Padding="15">
            <Grid>
                <TextBlock Text="PORTAL ZA VLASNIKE" 
                           FontSize="18" 
                           FontWeight="Bold" 
                           Foreground="White" 
                           HorizontalAlignment="Left"
                           VerticalAlignment="Center"/>
                <Button Content="Odjavi se" 
                        HorizontalAlignment="Right"
                        Background="#f44336"
                        Foreground="White"
                        Command="{Binding LogoutCommand}"/>
            </Grid>
        </Border>
        
        <TabControl Grid.Row="1" Margin="10">
            
            <!-- Tab za upravljanje hotelima -->
            <TabItem Header="Moji hoteli">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="2*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    
                    <Grid Grid.Column="0">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="200"/>
                        </Grid.RowDefinitions>
                        
                        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="10">
                            <Button Content="Učitaj hotele" Command="{Binding LoadHoteliCommand}"/>
                            <Button Content="Odobri hotel" Command="{Binding ApproveHotelCommand}" Background="#4CAF50" Foreground="White"/>
                            <Button Content="Odbij hotel" Command="{Binding RejectHotelCommand}" Background="#f44336" Foreground="White"/>
                        </StackPanel>
                        
                        <DataGrid Grid.Row="1" 
                                  ItemsSource="{Binding MojiHoteli}"
                                  SelectedItem="{Binding SelectedHotel}"
                                  Margin="10">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Šifra" Binding="{Binding Path=., Converter={StaticResource HotelSifraConverter}}" Width="100"/>
                                <DataGridTextColumn Header="Ime" Binding="{Binding Path=., Converter={StaticResource HotelImeConverter}}" Width="*"/>
                                <DataGridTextColumn Header="Zvezdice" Binding="{Binding Path=., Converter={StaticResource HotelZvezdiceConverter}}" Width="80"/>
                                <DataGridTextColumn Header="Status" Binding="{Binding Path=., Converter={StaticResource HotelStatusConverter}}" Width="100"/>
                            </DataGrid.Columns>
                        </DataGrid>
                        
                        <GroupBox Grid.Row="2" Header="Apartmani izabranog hotela" Margin="10">
                            <DataGrid ItemsSource="{Binding Apartmani}">
                                <DataGrid.Columns>
                                    <DataGridTextColumn Header="Ime" Binding="{Binding Path=., Converter={StaticResource ApartmanImeConverter}}" Width="*"/>
                                    <DataGridTextColumn Header="Sobe" Binding="{Binding Path=., Converter={StaticResource ApartmanSobeConverter}}" Width="60"/>
                                    <DataGridTextColumn Header="Gostiju" Binding="{Binding Path=., Converter={StaticResource ApartmanGostijuConverter}}" Width="60"/>
                                </DataGrid.Columns>
                            </DataGrid>
                        </GroupBox>
                    </Grid>
                    
                    <GroupBox Grid.Column="1" Header="Dodaj novi apartman" Margin="10">
                        <StackPanel Margin="10">
                            <Label Content="Ime apartmana:" FontWeight="Bold"/>
                            <TextBox Text="{Binding ApartmanIme, UpdateSourceTrigger=PropertyChanged}"/>
                            
                            <Label Content="Opis:" FontWeight="Bold"/>
                            <TextBox Text="{Binding ApartmanOpis, UpdateSourceTrigger=PropertyChanged}" 
                                     Height="80" 
                                     TextWrapping="Wrap" 
                                     AcceptsReturn="True"/>
                            
                            <Label Content="Broj soba:" FontWeight="Bold"/>
                            <TextBox Text="{Binding ApartmanBrojSoba, UpdateSourceTrigger=PropertyChanged}"/>
                            
                            <Label Content="Max broj gostiju:" FontWeight="Bold"/>
                            <TextBox Text="{Binding ApartmanMaxGostiju, UpdateSourceTrigger=PropertyChanged}"/>
                            
                            <Button Content="Kreiraj apartman" 
                                    Command="{Binding CreateApartmanCommand}"
                                    Margin="0,20,0,0"
                                    Background="#4CAF50"
                                    Foreground="White"
                                    FontWeight="Bold"/>
                        </StackPanel>
                    </GroupBox>
                </Grid>
            </TabItem>
            
            <!-- Tab za rezervacije -->
            <TabItem Header="Rezervacije">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    
                    <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="10">
                        <Label Content="Filter:" VerticalAlignment="Center" FontWeight="Bold"/>
                        <ComboBox SelectedItem="{Binding StatusFilter}" 
                                  Width="150"
                                  VerticalContentAlignment="Center">
                            <ComboBoxItem Content="Sve" IsSelected="True"/>
                            <ComboBoxItem Content="Na čekanju"/>
                            <ComboBoxItem Content="Potvrđeno"/>
                        </ComboBox>
                        <Button Content="Učitaj" Command="{Binding LoadRezervacijeCommand}" Margin="10,0"/>
                    </StackPanel>
                    
                    <DataGrid Grid.Row="1" 
                              ItemsSource="{Binding Rezervacije}"
                              SelectedItem="{Binding SelectedRezervacija}"
                              Margin="10">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="ID" Binding="{Binding Path=., Converter={StaticResource RezervacijaIdConverter}}" Width="100"/>
                            <DataGridTextColumn Header="Datum od" Binding="{Binding Path=., Converter={StaticResource RezervacijaDatumOdConverter}}" Width="100"/>
                            <DataGridTextColumn Header="Datum do" Binding="{Binding Path=., Converter={StaticResource RezervacijaDatumDoConverter}}" Width="100"/>
                            <DataGridTextColumn Header="Status" Binding="{Binding Path=., Converter={StaticResource RezervacijaStatusConverter}}" Width="100"/>
                        </DataGrid.Columns>
                    </DataGrid>
                    
                    <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="10" HorizontalAlignment="Center">
                        <Button Content="Potvrdi rezervaciju" 
                                Command="{Binding ApproveRezervacijaCommand}"
                                Width="150"
                                Background="#4CAF50"
                                Foreground="White"/>
                        <Button Content="Odbij rezervaciju" 
                                Command="{Binding RejectRezervacijaCommand}"
                                Width="150"
                                Background="#f44336"
                                Foreground="White"/>
                    </StackPanel>
                </Grid>
            </TabItem>
        </TabControl>
        
        <Border Grid.Row="2" Background="#F5F5F5" Padding="10">
            <TextBlock Text="{Binding StatusMessage}" FontSize="12"/>
        </Border>
    </Grid>
    
    <Window.Resources>
        <local:HotelSifraConverter x:Key="HotelSifraConverter"/>
        <local:HotelImeConverter x:Key="HotelImeConverter"/>
        <local:HotelZvezdiceConverter x:Key="HotelZvezdiceConverter"/>
        <local:HotelStatusConverter x:Key="HotelStatusConverter"/>
        <local:ApartmanImeConverter x:Key="ApartmanImeConverter"/>
        <local:ApartmanSobeConverter x:Key="ApartmanSobeConverter"/>
        <local:ApartmanGostijuConverter x:Key="ApartmanGostijuConverter"/>
        <local:RezervacijaIdConverter x:Key="RezervacijaIdConverter"/>
        <local:RezervacijaDatumOdConverter x:Key="RezervacijaDatumOdConverter"/>
        <local:RezervacijaDatumDoConverter x:Key="RezervacijaDatumDoConverter"/>
        <local:RezervacijaStatusConverter x:Key="RezervacijaStatusConverter"/>
    </Window.Resources>
</Window>
